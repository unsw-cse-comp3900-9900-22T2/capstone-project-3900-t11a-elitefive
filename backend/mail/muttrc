

set from = "yavalath3900@gmail.com"
set realname = "Yavalath 3900"

# IMAP
set imap_user = "yavalath3900@gmail.com"
set imap_pass = "uanyqlvurkvqptfr"

# SMTP
set smtp_url = "smtps://yavalath3900@smtp.gmail.com"
set smtp_pass = "uanyqlvurkvqptfr"

# Gmail folders 
set folder = "imaps://imap.gmail.com/"
set spoolfile = "+INBOX"
set postponed = "+[Gmail]/Drafts"
set record = "+[Gmail]/Sent Mail"
set trash = "+[Gmail]/Trash"

set header_cache     =~/.mutt/cache/headers
set message_cachedir =~/.mutt/cache/bodies
set certificate_file =~/.mutt/certificates

